# 2026-08-19 — the D010 wave

> One day, 31 commits `e3e0c3a..c85f756`, 1362 → 1394 tests, 70 → 73 harness
> groups plus arms in five existing ones, ten agent batches in two waves and a
> coordinator's batch, every one landed serially through the harness in a
> clean detached worktree. CI: red for eleven runs before the day, green on
> every push after the first. Kept because the commits say *what* and this
> says what the day kept finding.

## 1. Before anything: CI was red and nobody had looked / 시작 전: CI는 빨갛고 아무도 보지 않았다

Ten consecutive CI runs had failed while the local harness was green
throughout — the previous session's record says "every one landed serially
through the harness" and that was true, locally. Three causes, none in the
code: a byte comparison exempting a hand-listed padding range (CI's omniORB
pads differently), a fixed fixture port inside Linux's ephemeral range, an
unretried jar download. One commit, three negative controls, first green run
in eleven. **A landing is local until CI agrees, and "read the record before
planning" now includes `gh run list`.**

CI가 열 런 연속 빨강, 로컬 하네스는 내내 초록. 원인 셋, 코드에는 없음. **착지는
CI가 동의할 때까지 로컬이다.**

## 2. What the plan got wrong, found by building it / 계획이 틀린 것, 지어 보며 찾음

D010 was written from the gap columns and checked row by row against the
code the day before. Three of its class-A rows were still wrong in a way only
the batch could show:

| Row | D010 said | The batch found |
|---|---|---|
| A1 | review "corrected" it to `orbweaver-gen` only; the count at the old address is the oracle | `rt::Dispatch` is giop's trait re-exported and no `Dispatch` could ask for status 4; the count is **1 under both statuses** for our client and omniORB — it cannot go red; the status byte is the oracle |
| A3 | "a three-crate `Invoker::invoke` change" | not needed: the guard reads the stub's own bytes back through the contract; no stub, trait or emitted file changed |
| A5 | "§2's naming row says 13 of 16" | that number occurs in no document — the stale-count class, in the sentence describing it |

None of these was visible to reading. Each was found by the person who had to
make the assertion go red first. *D010 §3 carries all three corrections now.*

세 행 모두 읽어서는 보이지 않았고, 단언을 먼저 빨갛게 만들어야 했던 사람이 찾았다.

## 3. Class B measured red before it measured green / B류가 초록 전에 빨강으로 재어졌다

D010 said GIOP 1.1 wide text was unmeasurable here — omniORBpy cannot read its
own — and that the first step was a JacORB-at-1.1 fixture. The fixture took
one batch and found, the same hour, that at 1.1 we write a byte-order mark
JacORB neither writes nor strips: its user got `U+FEFF` + text, and its echo of
our mark came back as data that **our reader then stripped**, so
`spike-interop`'s "wstring round-tripped under GIOP 1.1" had been green with
the peer's user seeing the wrong value. Fixed the same day; the fixture asserts
the version from bytes and counts wire units so the masking cannot recur; and
JacORB reads an unmarked 1.1 value in *message* order, which contradicts a
literal reading of §9.3.1.6's "neither → big-endian" bullet — the code follows
the only 1.1 peer it can measure and says so.

That is the previous session's recurring finding once more — *a convention both
ends apply cannot be refuted by a round trip* — with a new corollary: **a
convention one end applies on read can hide the other end's defect on write.**

잴 수 없다던 B류 행이 픽스처 하나로 재어졌고, 첫 측정은 빨강이었다: 우리 리더가
피어의 되돌아온 데이터를 표식으로 벗겨내어 왕복이 초록이었다.

## 4. Empty witnesses, three of them / 빈 증인 셋

A4's Python batch reported that a value under `TypeCode::Recursive` cannot
cross AnyJSON in Rust — and that nothing was red because the sweep's recursive
witness was the empty list. The follow-up batch found the same emptiness in
the property sweep (`TreeSeq` was `[]` on every valued case and `None` on 22 of
32, skipped through a bare `continue` while the summary said 32) and in the
Python sweep, and reported a third in `ifr_reaches_the_agent.rs`. A valueless
property case is a `prop/unmeasured` finding the harness fails on now. And the
honest limit: the property sweep never calls `anyjson`, so it could not have
caught the defect regardless of witness.

재귀 증인이 세 곳에서 빈 목록이었다. 값 없는 속성 케이스는 이제 하네스가
실패시킨다. 속성 스윕은 `anyjson`을 호출하지 않으므로 어차피 잡을 수 없었다.

## 5. Documents that a script writes / 스크립트가 쓰는 문서

`SERVICES-COVERAGE.md` §8 is generated from `service_sweep.sh --raw` and diffed
by the harness; the sweep also names, for the first time, the 22 declared
interfaces it probes against no object. Every hand-typed count in §3–§7 was
stale and left the headings. The check went red by itself the same afternoon
when A2 added two operations to golden 22 — the negative control the gate got
for free. `gap_symbols.py` exists as the report D010 §7.1 demoted it to:
12 of 12 gap-column symbols exist in their crate, printed at the bottom of
every run as the reason it is not a gate.

Also: PLAN's five class-D rows restated or moved to aspirations; README's two
restated target tables gone; the standing/measured target confusion (85/98 vs
60/95) resolved into two facts with one home each.

## 6. Process, measured on the person running it / 절차, 운영자에게 잰 것

- **An agent that stops and is resumed loses its worktree and writes into the
  main checkout** — the 2026-08-14 failure mode, again, on the first agent of
  the day. Detected by `git status` in main; recovered as before (no writing
  git commands, report the file list, coordinator commits by path). Cost:
  every later landing went through a clean detached worktree
  (`corba-landing`) so the gate measured committed code only.
- **The harness named what a batch reported and did not fix.** A1 listed
  `spikes/bench/stub.rs` as a stale generated stub outside its footprint;
  `call-bench` refused to measure until it was re-blessed. That is the harness
  doing the codify step's audit for us.
- **Two agents' scratch files collided** in the shared scratchpad; one agent's
  red output was overwritten by another's. Prefix scratch files with the batch.
- Ten batches: first-pass green on 7; one round of repair on 3 (each a
  test-side cause: a literal's width, a witness's shape, an unannotated-effect
  policy ordering). Every landing carried its negative control in the message.

## 7. Numbers, and what they do not say / 숫자, 그리고 숫자가 말하지 않는 것

|  | 2026-08-19 morning | evening |
|---|---:|---:|
| tests | 1362 | 1394 |
| harness groups | 70 | 73 (+ arms in 5) |
| CI, last completed run | red (11th) | green (every push) |
| D010 class A | 5 open (A1–A5) | A1–A5 landed; A6 waits on a consumer |
| D010 class B | 6 | B5's first half measured (and it found a defect); B1–B4, B6 unchanged |
| Python round-trip values (golden / services) | 78 / 35 | 158 / 70 |
| SERVICES-COVERAGE numbers typed by hand | all | none |

Not measured today, still: identity through a real provider, SSLIOP against a
peer, a container, TAO, a real embedding model — the class-B rows whose
fixtures are not on this machine, exactly as D010 lists them. And the two
oracles two batches named and did not build: fallback-on-failure for
"permanent" (a second server at a second address), and a JSON leg in the
property sweep.
